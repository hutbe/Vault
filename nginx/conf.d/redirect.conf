# 将所有 HTTP 请求 80 重定向到 HTTPS 443
server {
    listen 80;
    server_name _;

    # 可根据需要排除某些路径 (例如 ACME challenge)
    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    return 301 https://$host$request_uri;
}